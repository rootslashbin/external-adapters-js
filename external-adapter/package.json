{
  "name": "@chainlink/external-adapter",
  "version": "0.2.6",
  "description": "Helpers for creating Chainlink External Adapters",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --ignore-path ../.eslintignore . --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "eslint --ignore-path ../.eslintignore . --ext .js,.jsx,.ts,.tsx --fix",
    "test": "yarn _mocha --exit --timeout 0",
    "test:unit": "yarn _mocha --exit --grep @integration --invert --timeout 0",
    "test:integration": "yarn _mocha --exit --grep @integration --timeout 0"
  },
  "license": "MIT",
  "dependencies": {
    "@google-cloud/logging-winston": "^4.0.1",
    "axios": "^0.19.2",
    "chai": "^4.2.0",
    "eslint": "^7.2.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-standard": "^4.0.1",
    "sinon": "^9.0.3",
    "uuid": "^8.3.0",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "express": "^4.17.1"
  }
}
